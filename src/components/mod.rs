mod project_card;
mod contact_form;
mod navbar;
mod footer;
mod particle_canvas;
mod education_card;
mod timeline_item;

pub use crate::{
    components::{navbar::*, footer::*, education_card::*,
                 timeline_item::*, project_card::*,
                 particle_canvas::*,
    }
};